name: Java CI with Maven

permissions:
  contents: read

on:
  pull_request:
    branches: ["*"]

jobs:
  call-maven-ci:
    uses: project-ncl/shared-github-actions/.github/workflows/maven-ci.yml@d96b556371f3d36b2f4656feb1e1d5b25162714e # main
    with:
      upload_artifacts: true
