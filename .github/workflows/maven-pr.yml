name: Java CI with Maven

permissions:
  contents: read

on:
  pull_request:
    branches: ["*"]

jobs:
  call-maven-ci:
    uses: project-ncl/shared-github-actions/.github/workflows/maven-ci.yml@510db8f4411bae66805ab0bebc2e4ad1670d06fb # main
    with:
      upload_artifacts: true
